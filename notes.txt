console.log("hit")
console.log(array);

var(--default-bar)
var(--index-bar)
var(--sorted-bar)
var(--min-bar)
var(--test-bar)
var(--pivot-bar)

# currently hosted via vercel with npm run build and config/
